# Anthropic API key (or leave unset and use `ant auth login`)
ANTHROPIC_API_KEY=

# Determinations below this confidence score are routed to human review
CONFIDENCE_THRESHOLD=0.85

# ChromaDB persistence directory
CHROMA_DIR=.chroma
